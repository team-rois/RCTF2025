module solution::solution {

    // [*] Import dependencies
    use sui::tx_context::{Self, TxContext};
    use sui::coin::{Self, Coin};

    public fun solve(_ctx: &mut TxContext) {
        // your solution here.

    }
}