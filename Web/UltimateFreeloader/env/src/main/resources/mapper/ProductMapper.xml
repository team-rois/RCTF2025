<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rois.happy_shopping.mapper.ProductMapper">

    <resultMap id="BaseResultMap" type="com.rois.happy_shopping.entity.Product">
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="price" property="price" jdbcType="DECIMAL"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, description, price, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.rois.happy_shopping.entity.Product">
        insert into products (id, name, description, price, create_time, update_time)
        values (#{id}, #{name}, #{description}, #{price}, #{createTime}, #{updateTime})
    </insert>

    <select id="findById" parameterType="java.lang.String" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from products
        where id = #{id}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from products
        order by create_time desc
    </select>

</mapper>
